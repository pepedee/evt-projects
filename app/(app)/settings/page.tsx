import { ComingSoon } from "@/components/shared/coming-soon";

export const metadata = { title: "Settings · AI Project Tracker" };

export default function SettingsPage() {
  return <ComingSoon title="Settings" phase={10} />;
}
