# TASKS — AI Project Tracker

Shared task list. Any tool working in this repo reads this first and updates it
at the end of every phase. Statuses: `todo` / `in-progress` / `done` / `blocked`.

Rule: finish and verify one phase before starting the next.

## Phases

| # | Phase | Scope | Status | Notes |
|---|---|---|---|---|
| 1 | Scaffold | Next 16 + Tailwind v4, both Supabase clients, migration runner, port 3006, docs | **done** | Verified at the time: build + lint clean, 0 npm vulnerabilities. `proxy.ts` (removed in `ac6d171`, restored in `0013_restore_auth.sql`'s commit) is back and live-verified — see phase 3. |
| 2 | Database | Migrations 0000–0008: `tracker` schema, workspaces + members, domain tables, RLS helpers + policies, expose_schema (APPEND), signup trigger | **done** | 2026-08-05: all 13 migrations (0000–0012) applied cleanly to the new dedicated project, zero errors. `npm run check-schemas` confirms `tracker` is exposed alongside `public`/`graphql_public`. |
| 3 | Auth + shell | Login/register via Supabase Auth (restored 2026-08-05), `lib/auth.ts`, protected `(app)` layout, sidebar, topbar, dark mode | **verified** | 2026-08-05: full loop confirmed live — created a confirmed account via the Admin API, signed in through the real `/login` form, dashboard showed the real identity ("signed in as Owner"), signed out via the topbar button, `/dashboard` correctly redirected back to `/login`. A second account in its own workspace saw `0 projects` from the first account's workspace — real RLS isolation, not just documented. Also: theme toggle flips the `dark` class, mobile drawer opens/closes with Escape support, skip-to-content is the first tab stop, `/demo` stays reachable without signing in. |
| 4 | Projects + tasks | `lib/db/projects.ts`, `tasks.ts`, list + detail + kanban, milestones, server-derived progress and health | **verified** | 2026-08-05: **the real "New project" form was completely broken until today** — see the `created_by`/`owner_id`/`assignee_id` bug below. Fixed and re-verified: submitting the actual form creates a real row. `progress_pct` trigger, overdue counting, and `project_overview` also confirmed via direct SQL. Kanban drag-and-drop specifically not exercised. |
| 5 | Budgets | Budget lines, expenses, planned-vs-actual rollups | **verified** | 2026-08-05: live checks confirm rollup math (planned 1000 / spent 550 / unassigned 150), the cross-project budget-line guard trigger fires, and deleting a budget line leaves its expenses unassigned rather than deleting them (`on delete set null`). |
| 6 | Files | Private bucket `project-files`, `lib/storage.ts`, upload button, 300s signed URLs | **verified** | 2026-08-05: live checks confirm upload, a working signed URL that returns the correct bytes, the raw/public URL being refused (bucket is private, confirmed 400), and clean deletion. Upload-button UI and viewer-role RLS denial not exercised. |
| 7 | Dashboard | KPI cards, charts, upcoming + overdue, recent activity | **verified** | 2026-08-05: seeded a live project and confirmed dashboard KPI tiles ("Active projects", "Open tasks") match the seeded counts exactly. Chart rendering/theming not re-verified visually this session — see `/demo` for that (step 27, done pre-database). |
| 8 | AI summaries | `lib/ai/*`, streaming route, caching by `input_hash` | **verified** | 2026-08-05: `ANTHROPIC_API_KEY` added. Live-verified all three behaviors: fresh generation (`cached:false`, real factually-grounded text matching the seeded project), replay from cache on an identical re-request (`cached:true`), and regeneration after editing a task (`input_hash` correctly invalidates). Bundle scan re-confirmed clean. Caching was actually broken until the `created_by` fix below — every call was silently regenerating. |
| 9 | Word reports | `lib/reports/word.ts` with `docx`, download route | **verified** | 2026-08-05: `GET /api/reports/word?projectId=...` against a live project returns HTTP 200, correct OOXML content-type, and a valid ZIP-signed (`PK`) .docx — both with `?ai=0` and with no `ANTHROPIC_API_KEY` set at all, confirming AI sections are truly best-effort and never block the download. |
| 10 | Polish | Empty/loading/error states, activity log view, responsive audit, README | **verified** | Build + lint clean. Skip-to-content confirmed as the first tab stop; mobile drawer opens/closes correctly with Escape support; no horizontal overflow at 375 / 768 / 1280 on the live dashboard. `/demo` (fixture-based) verified separately. Not covered: settings/activity-log page interactions, and a full drag-and-drop kanban pass. |
| 11 | Handover report | `documents.category`, `tasks.kind` ('defect'), `projects.location`, AI-drafted handover summary, `lib/reports/handover.ts` with embedded photos + listed documents, download route + UI entry point | **verified** | 2026-08-06: construction-industry completion/handover report (project overview, scope of work, as-built drawings, inspection certificates, defect/snag list, warranties, O&M manuals, financial summary). Photos embed inline via `docx`'s `ImageRun` (jpg/png/gif only — webp and non-images list as unembeddable rather than failing the report); drawings/certificates/warranties/O&M manuals are listed by name. Defects reuse `tasks.kind` rather than a separate table. Shared Word-building helpers extracted to `lib/reports/shared.ts`, used by both this and the original status report. Verified live end-to-end with a throwaway test account: uploaded a real photo through the actual UI (category picker), confirmed the `.docx`'s ZIP contains a real `word/media/*.jpg` entry (not just a non-erroring response), and the AI-drafted handover summary read correctly from the project record while correctly omitting sections it wasn't asked to write. Test account and orphaned storage objects cleaned up after. **`projects.location` was silently unreadable through `project_overview` until 0016** — see phase 12. |
| 12 | Import project from PDF | `lib/ai/import.ts` (forced tool-use extraction), `app/api/projects/import/route.ts`, `components/projects/import-form.tsx` (upload → editable review → save), `createProjectFromImport` action, `0016_project_overview_location.sql` | **verified** | 2026-08-06: upload a quotation PDF, Claude extracts a draft project (name/code/client/location/description/currency/budget lines/tasks) via a forced tool call, user reviews and edits every field before anything is saved — nothing is written until the review form is submitted. PDF only for v1 (Claude's document content block doesn't take .docx/.xlsx directly). Verified live against a real quotation (EVT26QT034): every extracted field matched the source document exactly, including the money figures. **Found and fixed a real bug while verifying**: `tracker.project_overview` was built with `select p.*` before `projects.location` existed (0015); Postgres locks a wildcard's column list at `CREATE VIEW` time and never auto-updates it, so `location` was writable but always read back as `undefined` through `getProject()`/`listProjects()` — the Edit page's pre-fill and the handover report's Location row were both silently blank. Fixed in `0016_project_overview_location.sql` (drop + recreate, since `CREATE OR REPLACE VIEW` can only append columns at the very end and `location` wasn't last). Guard note added to `AGENTS.md` so a future column addition doesn't repeat this. |
| 13 | Task due dates, inline | `setTaskDueDate` action, editable date `<input>` on each row in `TaskList` and `TaskBoard` | **verified** | 2026-08-06: due date could only ever be set at task creation — no way to add or change one on an existing task. Added a dedicated action (same pattern as `setTaskStatus`: partial update, not the full `updateTask` schema) and an inline date input in both the list and kanban views. Verified live: set a date on a task with none, confirmed the exact value persisted in Postgres (`to_char` check, sidestepping a red-herring UTC/local-midnight artifact in my own verification script's JS `Date` handling — the stored value was correct all along), and confirmed the UI's relative-date label ("in 14 days") updated after reload. |
| 14 | Budget line editing | `patchBudgetLine` (partial update, replaces the original `updateBudgetLine` full-row action) wired into `BudgetLines` as inline category/amount inputs | **verified** | 2026-08-06: the action existed since phase 5 but nothing called it — only add and delete were reachable from the UI. Added inline editable inputs, saved on blur. **Found and fixed a real bug live while verifying**: category and amount inputs shared one `pending` flag with the whole list (add form, delete, every row); editing category disabled every other input — including the amount field — for the duration of that save's round trip, so a fast edit-then-edit-the-next-field sequence silently dropped the second edit entirely (confirmed by reproducing it, then fixing, then reproducing the same fast sequence again and confirming both fields saved). Fixed by not disabling these two inputs on the shared `pending` flag — a failed save still surfaces through the existing error `Notice`, so blocking input during a background save was never load-bearing. **Retrofitted after phase 15**: `updateBudgetLine` had the same full-row-resend race that phase 15 found and fixed in expenses (two fields on one row, later stale-snapshot save silently overwriting an earlier concurrent edit). Replaced with `patchBudgetLine`, a true partial update, same shape as `patchExpense`. Verified live with a fresh throwaway account: typed a new category, then — before that save's round trip landed — immediately triple-clicked and retyped the amount field; after both blurred, a hard page reload confirmed both the new category ("Materials") and new amount (2,500) persisted correctly with neither stomping the other. |
| 15 | Expense editing | `patchExpense` (new, true partial update — no `updateExpense` existed before this), inline description/budget-line/date/amount editing in `ExpenseList` | **verified** | 2026-08-06: expenses only ever supported add and delete; there was no update action of any kind. Added inline editing for all four fields. **Found and fixed a second, more subtle version of phase 14's race** while verifying: the first implementation avoided the shared-`pending`-disables-everything bug by not disabling inputs, but still resent the *whole row* (reconstructed from the component's `expense` prop) on every single field's save. With four independently-editable fields on one row, editing two different fields within the same fraction of a second — realistic: type an amount, then click a different field before the first save's server round trip and revalidation complete — let the second save's stale snapshot silently overwrite the first field's just-saved value, even though neither save touched the other's field directly. Reproduced it live (amount edit lost after a concurrent budget-line change), then fixed it properly: `patchExpense` only `UPDATE`s the columns actually passed, so two concurrent single-field saves can never stomp each other regardless of ordering. Reproduced the identical fast sequence again afterward and confirmed both fields now save correctly. Worth remembering for any future multi-field-per-row inline editing in this app: **partial-field updates, not whole-row resends, or this exact bug returns.** |
| 16 | Invite members by email | `0017_invites.sql` (`workspace_invites` table + RLS, `provision_workspace()` checks for a pending invite before making a new workspace), `inviteMember`/`cancelInvite` actions, `InvitePanel` on `/settings` | **verified** | 2026-08-06: a newly registered account always got its own brand-new workspace — there was no way to bring a second person into an existing one, so a colleague signing up could never see the owner's projects. Added an email invite: an admin+ enters an email and a role on `/settings`; no email is sent (out of scope — would need Supabase SMTP/template config — **superseded by phase 17, which adds real email delivery**), the owner tells the person to register with that exact address. `provision_workspace()` (originally 0007) now checks `workspace_invites` for a pending row matching the new signup's email before falling back to creating them their own workspace; a match adds them straight into the inviting workspace at the invited role and marks the invite accepted. Deliberately scoped to *not-yet-registered* invitees only — an already-registered person joining a second workspace would need multi-workspace membership and a workspace switcher, neither of which this app has (`getSessionUser` picks one membership row, oldest first) — **built in phase 18**. Inviting as `owner` is restricted to existing owners (checked in both the action and the RLS insert policy) so an admin can't mint a co-equal role for themselves. Verified live end-to-end with throwaway accounts: seeded an owner with one project, invited `invitee-test@example.com` as Admin through the real `/settings` form (visible immediately in the pending-invites list and the activity log), then created that exact account via the Admin API to fire the real signup trigger — confirmed via direct SQL that the new user landed in the *owner's* `workspace_id` with role `admin` (not a new workspace) and the invite row's `accepted_at` was set. Signed in as the invitee in the browser: dashboard read "Owner Tester's workspace · signed in as Admin" with the owner's project visible, and adding a budget line on that project succeeded, confirming RLS actually grants write access, not just UI visibility. Both test accounts deleted after (cascades the whole test workspace via `workspaces.created_by`). |
| 17 | Invite email delivery | `lib/supabase/admin.ts` (the one narrow, deliberate service-role exception — see AGENTS.md), `inviteMember` now calls `auth.admin.inviteUserByEmail`, `app/(auth)/accept-invite/` (page + client form) | **verified** | 2026-08-06: phase 16 shipped invites that only took effect if the person self-registered later — the owner had to tell them manually. This phase makes the invite actually send a real email with a working link. `inviteMember` still writes the `workspace_invites` row first (so self-registration still works as a fallback if email delivery fails), then calls `auth.admin.inviteUserByEmail`, which creates the `auth.users` row immediately — the trigger honors the pending invite right away — and asks Supabase to send the email. An `email_exists` result means the address is already registered elsewhere; the dead pending-invite row is deleted in that case since `provision_workspace()` would never consume it. **Two real bugs found and fixed while verifying, neither guessable without testing against the actual project:** (1) checked the invite link's real shape with `auth.admin.generateLink` before writing any client code, and found it redirects with the session tokens in the URL **hash** (`#access_token=...`), not a `?code=` query param — `@supabase/ssr`'s browser client hardcodes `flowType: "pkce"` (read directly from its source), and a PKCE client's automatic `detectSessionInUrl` only recognizes `?code=`, so the hash was being silently ignored and no session ever appeared. Fixed by having `accept-invite-form.tsx` parse `window.location.hash` itself and call `auth.setSession()` explicitly rather than trusting auto-detection. (2) Even after that fix, one test run showed the *wrong* account signed in after accepting — confirmed via the raw session cookie that a session really had been established correctly, just not the one the UI reported. Root cause: React Strict Mode double-invokes effects in development, and Supabase refresh tokens are single-use and rotate on redemption — the first `setSession()` call succeeded, the second (now-stale) one failed, and whichever `.then()` resolved last won the race for the component's status. Fixed with a `useRef` guard so only the first invocation's call actually fires. Verified clean end-to-end after both fixes, in a fresh browser tab with no prior session: generated a real invite link, followed the redirect via `curl` to get real tokens (no live inbox available in this environment — same limitation noted for the original signup-confirmation email, sidestepped the same way), loaded `/accept-invite` with those tokens, confirmed via the session cookie that the *correct* invitee was signed in, set a password, landed on `/dashboard` correctly attributed ("Owner Four's workspace · signed in as Viewer"), then signed out and signed back in with the new password to prove the account is fully usable going forward — not just transiently authenticated by the magic link. Real email delivery itself couldn't be end-to-end verified (Supabase's shared mail sender rejects the `example.com` test domain and separately rate-limits after a few sends), but the failure path was exercised for real and degrades exactly as designed: the invite row and the account both still get created, the UI says plainly that the email failed, and self-registration with that address still works. All ten throwaway test accounts from this phase's iteration deleted after. |
| 18 | Multi-workspace membership + switcher | `getSessionUser()` cookie-aware active-workspace resolution (`ACTIVE_WORKSPACE_COOKIE`), `switchWorkspace` action, `listMyWorkspaces()`, workspace `<Select>` in `Shell`; `inviteMember`'s `email_exists` branch now adds the existing account to `workspace_members` directly (via `generateLink({type:"magiclink"})` to resolve their user id, no email sent) instead of erroring | **verified** | 2026-08-06: closed the gap phase 16 explicitly left open — inviting an already-registered email now works, by adding them to `workspace_members` directly and letting them switch into the new workspace from a dropdown that only appears once someone actually belongs to more than one (almost everyone never sees it). **This surfaced a serious, previously-latent bug while verifying**: every `lib/db/*` read function (`listProjects`, `getProject`, `listMilestones`, `listTasks`, `getTask`, `listBudgetLines`, `listExpenses`, `listDocuments`, `getDashboard`, and `listMembers`/`listPendingInvites`/`listActivity` in `lib/db/workspace.ts`) relied *only* on RLS's `is_member(workspace_id)` for scoping, with no explicit `workspace_id` filter of its own — safe by accident until now, since every user had exactly one membership so "a workspace I belong to" and "my active workspace" were the same thing by construction. The instant a user could belong to two, RLS correctly allowed reading *either* workspace's rows, and every list query silently combined both: caught live as "Active projects: 2" on a dashboard that should have read 1, and confirmed further that Settings' People/pending-invites/activity-log would have mixed two workspaces' data together too. Fixed by threading an explicit `workspaceId` parameter into every one of these functions (and `lib/ai/summarize.ts`'s snapshot/cache lookups) and adding `.eq("workspace_id", workspaceId)` to each query — RLS stays the backstop, the application is now what decides *which* of a user's permitted workspaces a given request actually means, which is the standard shape for this once tenancy stops being 1:1. Verified live end-to-end: seeded an owner (with one project) and a second, already-registered user (with their own separate workspace and project), invited that user's email from the owner's Settings page, confirmed the success message and the direct `workspace_members` insert, signed in as the invited user and confirmed the dashboard read "Active projects: 1" (not 2) by default, switched to the new workspace via the dropdown and confirmed both the KPI count and the visible project were now correctly scoped to *that* workspace, switched back and confirmed the reverse, and confirmed that navigating directly to the other workspace's project by URL while active in the wrong one now correctly 404s instead of leaking through. Both test accounts deleted after. **Any future `lib/db/*` query must filter by `workspace_id` explicitly — never assume RLS alone is enough, exactly the lesson this bug teaches.** |
| 19 | Dashboard progress: show all, with a filter | `getDashboard()`'s `progress` list uncapped (was top 8), `components/dashboard/progress-filter.tsx` (client-side status filter wrapping `ProgressChart`) | **verified** | 2026-08-06: the Project progress card silently dropped any project past the 8th by completion — with more than 8 live projects there was no way to see the rest. Removed the cap so every live project with at least one task shows, and added a status filter (Planning/Active/On hold — the only statuses that can appear here, since completed/cancelled are already excluded upstream) so a workspace with many projects can still narrow the view. Filters client-side rather than through the URL like `/projects`/`/tasks` do: the full list is already fetched for the KPI tiles above it, so refiltering is instant and doesn't reload the whole dashboard to toggle one card. Verified live: seeded 10 projects (6 active, 2 planning, 2 on hold, each with one done and one open task) on a throwaway account — dashboard read "10 of 10" where it used to silently cap at 8, filtering to "On hold" showed exactly the 2 matching projects, filtering to "Planning" showed the other 2, both with no page navigation (URL and title unchanged throughout). Test account deleted after. |
| 20 | Visual theme picker (macOS / iOS 26) | `[data-theme]` on `<html>`, a second axis from `.dark` — `app/globals.css` (macOS and iOS 26 variable sets, radius overrides, glass chrome), `components/layout/theme-style-switcher.tsx`, pre-paint script in `app/layout.tsx` extended to apply the stored style before first paint | **verified** | 2026-08-06: added two extra visual styles alongside the existing light/dark, picked from a dropdown in the topbar — "macOS" (tighter corner radius, system blue accent, translucent frosted sidebar/header) and "iOS 26" (heavier glass blur, larger radius, pill-shaped buttons/inputs, subtly elevated cards). Chart colours (`--chart-1`/`--chart-2`/`--chart-critical`/etc.) are untouched by either theme — those were validated for colour-vision deficiency and contrast (see "Charts" in AGENTS.md) and this doesn't redo that work, only UI chrome varies. Corner radius and glass are applied via CSS rules scoped to `[data-theme=...]` targeting the existing Tailwind utility classes (`.rounded-2xl`, `button.rounded-lg`, etc.) directly, rather than threading a radius variable through every component — no component code changed. Glass/blur is deliberately limited to the sidebar and header; ordinary cards stay opaque, since this app's tables carry money and dates and blurring what sits behind them would cost real legibility for a cosmetic flourish. **Found and fixed a real bug live while verifying**: the picker's `<select>` was originally controlled by React state initialized from a lazy `useState` reading the real `data-theme` attribute — the same pattern used successfully elsewhere in this codebase for reading DOM-only state. For this specific element it silently failed: the DOM attribute updated correctly but the `<select>`'s own visible selection reverted back to "Classic", confirmed by directly inspecting `select.value` after the fact. Root cause is a known React limitation — hydrating a controlled `<select>` whose computed initial value disagrees with the server-rendered option (the server can never know the stored preference) is unreliable, unlike plain text or CSS-driven content. Fixed by following `ThemeToggle`'s existing pattern exactly: an uncontrolled `<select defaultValue="classic">` (so the very first client render matches the server exactly, zero mismatch), corrected imperatively via `ref.current.value = ...` in an effect afterward — a real DOM synchronization, not state React should own. Verified live after the fix: `data-theme` and the select's own value stayed in sync across a fresh reload, both new themes render correctly in light and dark, reverting to "Classic" correctly removes the attribute, and a dense page (project detail with tasks/budget tables) stays fully readable under iOS 26's heaviest glass setting. Test account deleted after. |
| 21 | Materials/procurement tracking + QC inspection grid | `0018_qc_and_materials.sql` (`materials`, `qc_items`, `qc_units`, `qc_results` tables + RLS), `lib/db/materials.ts`, `lib/db/qc.ts`, `app/(app)/materials/actions.ts`, `app/(app)/qc/actions.ts`, `components/materials/material-list.tsx`, `components/qc/qc-grid.tsx`, both wired into the project detail page | **verified** | 2026-08-06: found missing by comparing against a real, already-in-use standalone HTML tracker for one specific project (36-unit cooling-tower pipe support repair). Two features that project needed and this app didn't have: (1) **Materials** — item, qty × unit cost (total computed, never stored), supplier, and a needed/ordered/received/installed status; deliberately a separate table from `budget_lines`/`expenses`, since those track planned-vs-actual money and this tracks procurement state, a different axis. (2) **QC inspection grid** — rows are work items, columns are physical units (e.g. `CT-01`..`CT-36`), each cell independently pass/fail/not-yet-inspected. Modelled as three tables (`qc_items`, `qc_units`, `qc_results`) rather than a positional array per item, specifically so adding/removing/reordering a unit or item can never silently shift another row's results out of alignment — "not yet inspected" is the absence of a `qc_results` row, not a third enum value, so clicking a cell back to blank is a real delete. Clearing a cell is gated at `member` role rather than `admin` (unlike deleting a budget line or a QC item/unit) — cycling a checkbox-like cell during routine inspection work isn't the same kind of destructive action as deleting a whole row, so it doesn't need the same guard. Verified live on a throwaway project: added a material (qty 36 × ฿450 unit cost → confirmed the table shows ฿16,200.00 total, computed not stored), added one QC item and one unit, clicked the resulting cell through all three states (blank → green/passed → red/failed → blank) confirming each transition against the database directly (`qc_results` row appears on the first two clicks, is genuinely deleted — not just cleared — on the third), then deleted the item and confirmed it stayed gone. One real bug caught while first verifying: the initial add attempt via loosely-targeted click coordinates silently failed (an empty materials table with no error) — traced to stale element references after a scroll; fixed by re-reading the page for fresh refs before every interaction, not an app bug. Test account deleted after. |

| 22 | Fix: uploads over ~4.5MB failed with 413 in production | `components/files/upload-button.tsx` now uploads straight from the browser to Supabase Storage via the browser client, instead of POSTing to a `/api/upload` route (deleted) that proxied the bytes through a Vercel serverless function | **verified** | 2026-08-07: reported live — the app's own limit (`MAX_UPLOAD_BYTES` in `lib/storage.ts`) and the bucket's `file_size_limit` (0011_storage.sql) both allow 20MB, but anything over roughly 4.5MB failed with a plain `Upload failed (413)`. Root cause: Vercel's Node.js Serverless Functions cap a request body at 4.5MB, a platform ceiling that doesn't care what the app itself permits — and it's invisible on `npm run dev`, since localhost has no such limit, which is exactly why phase 6's original verification never caught it. `recordDocument`'s own doc comment already described "the browser has already put the bytes in the bucket" as the intended design, so the fix aligns the code with that: the browser uploads directly via Storage RLS (0011_storage.sql), which reads workspace membership out of the object path the same way regardless of whether the request originates from the browser or a server — proxying through Vercel was never actually the security boundary. Verified live against the real production deployment (not just localhost, since the 4.5MB ceiling only exists there): uploaded a synthetic 6,291,456-byte (6MB) file through the real UI, confirmed via direct SQL that `documents.size_bytes` recorded the exact byte count, uploaded a small file too to confirm the common case still works. **A second, separate, real issue surfaced while verifying, deliberately not folded into this fix**: every page throws a React hydration error (`#418`) in the production deployment specifically — reproduces on totally unrelated pages (`/dashboard`, `/demo`) ruling out any single component, does *not* reproduce in an identical local production build (`next build && next start`) or in dev, and doesn't appear to break anything observable (uploads, forms, and navigation all still work; React silently recovers by re-rendering client-side). Suspected `formatDate`/`formatDateTime`/`formatRelativeDays` using the runtime's ambient timezone rather than a pinned one (UTC on Vercel's server vs. the visitor's real timezone in the browser, so a timestamp near a day boundary could render different calendar dates and mismatch) — genuinely a real bug and fixed regardless (now pinned to `Asia/Bangkok`, see `lib/format.ts`), but deploying that fix alone did **not** make the console error go away, so either that wasn't the actual cause or there's an additional one. Given it reproduces only on Vercel specifically (not an identical local build), the remaining suspects are something about Vercel's own edge/streaming response handling rather than this app's code — left as an open, non-blocking item rather than guessed at further. |

| 23 | Import milestones from an MS Project XML export | `fast-xml-parser` dependency, `lib/import/msproject.ts` (deterministic MSPDI-schema parsing, no AI), `parseMsProjectMilestones`/`importMilestones` actions in `app/(app)/projects/actions.ts`, `components/projects/milestone-import.tsx`, wired into the Milestones card | **verified** | 2026-08-07: reads MS Project's "File > Save As > XML" export (the documented MSPDI schema — not the native `.mpp` binary, which has no reliable parser for this stack; a PDF export was also considered but XML's plain, documented schema makes deterministic parsing possible instead of another AI-extraction feature). A `<Task>` counts as a milestone when `<Milestone>1</Milestone>` and it isn't a `<Summary>` row; due date comes from `<Finish>`, falling back to `<Start>` if missing; status is derived from `<PercentComplete>` (100 → done, >0 → in_progress, else pending). Two-stage upload-then-review, matching the existing whole-project PDF import's pattern: parsing returns a draft list nothing is written from, the person can uncheck rows, edit any name/date/status, then only the checked rows get bulk-inserted in one action call. Verified live: a sample XML with a summary row, a non-milestone task, and four real milestones (one testing the `&amp;` entity in a name, one testing the Start-date fallback, one each at 0/50/100 percent complete) correctly extracted exactly the four milestones with the right names, dates, and statuses; unchecked one in the review step and confirmed only the remaining three were written, both via direct SQL (`to_char` to rule out a date being off by one from timezone-naive display, which it wasn't) and after a full page reload. Also verified the error path: a well-formed but unrelated XML file produces a plain, specific message ("No `<Project><Tasks>` found — is this really an MS Project XML export... not the native .mpp file?") rather than a stack trace. Test account deleted after. |

| 24 | Fix: pages loading slowly | `vercel.json` (`{"regions": ["syd1"]}`), `loading.tsx` added for `/budgets`, `/files`, `/settings` (the three routes that didn't already stream a skeleton) | **verified** | 2026-08-07: reported live. Root cause was a regional mismatch, not application code — this app has almost no static pages (nearly every route is server-rendered per-request against the database), and with no `vercel.json` its serverless functions defaulted to Vercel's `iad1` (US East, Virginia), while the Supabase database has always lived in `ap-southeast-2` (Sydney, confirmed from `SUPABASE_DB_URL`'s pooler hostname). Every page load was paying a Virginia↔Sydney round trip for its database queries on top of the visitor's own round trip to Virginia from Thailand — invisible in local development, where there's no network hop to either region at all. Pinned function execution to `syd1` (Sydney), co-located with the database, so only the one real hop (visitor↔Sydney) remains. Verified with matched before/after measurements against the live production deployment (not localhost — the whole issue only exists there): confirmed the actual execution region via the `x-vercel-id` response header (`sin1::iad1::...` before, `sin1::syd1::...` after — the first segment is the edge POP that's unaffected, the second is where the function actually ran), and timed five consecutive authenticated `/dashboard` loads with identical methodology before and after — averaged **~2180ms before, ~537ms after, roughly a 4× improvement**. Also added `loading.tsx` to the three routes that were still loading blank while their data fetched (dashboard/projects/project-detail/tasks already had one) — doesn't change actual latency, but the skeleton now appears immediately instead of a blank page during the (now much shorter) wait. |
| 25 | Hover tooltips for icon-only buttons | `components/ui/tooltip.tsx` (new `Tooltip` component), wired into every icon-only interactive control: `theme-toggle.tsx`, `shell.tsx` (Sign out), `budget-lines.tsx`, `expense-list.tsx`, `material-list.tsx`, `milestone-list.tsx`, `task-list.tsx` (archive), `document-list.tsx` (download + delete), `invite-panel.tsx` (cancel invite), `qc-grid.tsx` (delete unit, delete item, and every pass/fail cell), `import-form.tsx` (remove budget line/task rows) | **verified** | 2026-08-10: requested as "make 'Hint' show information on mouse over" — the app already had an always-visible `hint` prop on `Field`/`StatTile`, but converting those to hover-only would hurt touch/mobile users who can't hover at all, so per the user's explicit choice this built a new, separate `Tooltip` for icon-only controls instead, leaving the existing hint text untouched. First version was pure CSS (`group` + `group-hover`/`group-focus-within`, absolutely positioned relative to the trigger) — kept it dependency-free per this repo's "no dependency for a dozen lines of TypeScript" rule. Verified live on a throwaway project (`tooltip-test@example.com`): the Sign Out button's tooltip rendered correctly on both mouse hover and keyboard focus (tabbing to the button revealed it via `focus-within`, confirming it's accessible without a mouse). **Found a real bug while verifying the budget-lines table specifically**: the tooltip on the delete-line button was visibly clipped, both on the right and the bottom, inside its `overflow-x-auto` wrapper — confirmed empirically, not just a theoretical risk noted in the original design comment. Per the CSS spec, setting only `overflow-x: auto` implicitly computes `overflow-y: auto` too, so an absolutely-positioned tooltip escaping the trigger's box gets clipped by the container regardless of `z-index`. Rewrote `Tooltip` as a small client component that reads the trigger's `getBoundingClientRect()` on hover/focus and renders the bubble into a `document.body` portal with `position: fixed` coordinates (clamped to the viewport horizontally) — still no external dependency (`react-dom`'s own `createPortal`), but now escapes any scrollable ancestor entirely. Re-verified after the fix: the budget-line delete tooltip now renders in full below the table, unclipped; added a real QC item + unit to the same test project and confirmed the per-cell tooltip (`"CT-01: Not recorded"`) also renders correctly with no layout disruption to the small grid cells. `npm run lint` and `npm run build` both clean (20/20 routes). Test account, project, and temp scripts deleted after. |
| 26 | Quotation date per project | `0019_project_quotation_date.sql` (`projects.quotation_date date`), `0020_project_overview_quotation_date.sql` (drop/recreate `project_overview`, same gotcha as 0015/0016), `lib/db/projects.ts` (`ProjectRow`), `app/(app)/projects/actions.ts` (zod schema), `components/projects/project-form.tsx` (new field, paired with Location), `app/(app)/projects/[id]/page.tsx` (shown in the header meta line), `lib/reports/word.ts` + `lib/reports/handover.ts` (new row in each overview table), `lib/ai/import.ts` + `components/projects/import-form.tsx` (extracted from the quotation PDF during import, editable in review) | **verified** | 2026-08-11: requested as "Add date of Quotation on each project" — the date printed on the quotation/PO a project came from, distinct from `start_date` (when work actually begins, often much later). Followed the exact migration pattern `location` used (0015/0016): additive column, then a required follow-up migration dropping and recreating `project_overview` since Postgres expands `select p.*` into a fixed column list at `CREATE VIEW` time and never auto-picks-up a new column (see AGENTS.md). Placed the form field next to Location in a paired 2-column row rather than full-width, matching that field's visual weight. Also wired into the AI PDF-import path (`lib/ai/import.ts`'s `record_project` tool schema) since this app already extracts other quotation fields (client, location, budget lines) the same way — the extracted date is coerced to `null` rather than failing the whole import if the model returns something that isn't a clean ISO date, since it's a nice-to-have the reviewer can fill in by hand. Verified live on a throwaway project (`quotedate-test@example.com`): entered `2026-08-01` in the New Project form, confirmed it saved and round-tripped correctly — displayed as "No client · Quotation 01 Aug 2026" in the detail page header, and pre-filled correctly (`01-Aug-2026`) back in the Edit form. Both the Word report and handover report endpoints returned 200 with the new field wired into their overview tables (mirrors the already-proven `formatDate`/`table` pattern used for Start/Target date and Location, not independently content-verified beyond the successful response). `npm run lint` and `npm run build` both clean (20/20 routes) after fixing two now-incomplete `Project` fixtures (`app/demo/fixtures.ts`, `scripts/preview-report.ts`) the type checker caught. Test account and its cascaded project deleted after (confirmed no orphaned row). |
| 27 | Fix: layout broke on iPad and phone widths | `components/layout/shell.tsx` (`min-w-0` on the mobile topbar's workspace-name wrapper), base `grid-cols-1` added to every responsive grid container across the app (`app/(app)/dashboard/{page,loading}.tsx`, `app/(app)/projects/{page,loading}.tsx`, `app/(app)/projects/[id]/{page,loading}.tsx`, `app/(app)/tasks/loading.tsx`, `app/demo/page.tsx`, `components/tasks/task-board.tsx`, `components/projects/{project-form,import-form}.tsx`) | **verified** | 2026-08-11: requested as "make this webapp also fit on iPad, mobile phone." The shell already had a real responsive drawer nav (built early in the project) — the actual bugs were two specific, empirically-confirmed overflow sources, not a missing mobile design: (1) the topbar's workspace-name wrapper was `flex-1` without `min-w-0`, so on a narrow screen the flex item grew to fit the full untruncated name instead of shrinking, pushing the theme switcher/toggle/sign-out buttons off the right edge of the viewport — confirmed via `getBoundingClientRect()` showing the action group's right edge at 432px against a 375px viewport. (2) Systemic: every responsive grid in the app used a Tailwind pattern like `grid gap-4 sm:grid-cols-2` with no base `grid-cols-1` — below the first breakpoint this leaves `display:grid` with no `grid-template-columns` at all, so the browser falls back to an implicit column track sized by `auto` (effectively max-content), which grows past the container instead of being capped the way an explicit `grid-cols-1` (`minmax(0,1fr)`) would. Caught concretely on the Projects card grid: the grid container itself measured 343px (correctly within its parent), but the single card inside it rendered at 444px — 101px past its own container's edge, clipping the health badge and progress percentage off-screen. Fixed by adding an explicit `grid-cols-1` base to every one of the ~20 occurrences of this pattern found via `grep`, which is always a no-op change in intent (single column below the first breakpoint was already the visual goal) but forces the browser to actually cap the track width. Verified live end-to-end on a throwaway project seeded with realistic data (tasks, budget lines, materials, a QC grid with results) at three widths — mobile (375×812), iPad portrait (768×1024), iPad landscape (1024×768, where the sidebar switches from drawer to permanent per the existing `lg:` breakpoint) — walking every major page (dashboard, projects list, project detail including budget/materials/QC tables, tasks kanban board, budgets, settings) plus the mobile drawer nav itself (confirmed opens/closes correctly via direct DOM inspection after the `computer` tool's touch-emulation clicks proved unreliable in this environment — a tool quirk, not an app bug). Wide data tables (budget lines, materials, QC grid) correctly scroll horizontally within their own `overflow-x-auto` card rather than being force-fit or breaking the page — left as-is, that's the right behavior for dense tabular data on a phone. `npm run lint` and `npm run build` both clean (20/20 routes). Test account and its project deleted after. |
| 28 | Sortable Budgets table | `app/(app)/budgets/page.tsx` (sort/dir URL params, click-to-sort column headers) | **verified** | 2026-08-22: requested as "can we sort budget." Sorting is URL-driven (`?sort=planned&dir=desc`) and computed server-side over the already-fetched rows, matching the filter pattern already used on `/projects`/`/tasks` rather than adding client-side state — no new client component needed. Clicking a column header sorts by it (numeric columns default to descending — biggest first — since that's the more useful first click for money; name/status default to ascending); clicking the same header again flips direction, shown with an arrow icon. Rows with no planned budget (`used` is `null`) always sort last regardless of direction, since there's no meaningful rank for "n/a". Documented a real limitation in a code comment rather than hiding it: Planned/Spent/Variance sort by raw number, which only ranks correctly when every row shares a currency — this workspace is almost entirely THB with one new USD project (phase 29's Anh Minh Vietnam import), so it's a live edge case, not just theoretical, but fixing it properly (per-currency grouping or FX conversion) was judged out of scope for a plain sort feature. Verified live on a throwaway account seeded with three projects at deliberately distinct planned amounts (10,000 / 100,000 / 900,000 THB, one intentionally over budget): confirmed Planned ascending/descending both ordered correctly, confirmed the toggle direction in each link's own href, confirmed Name ascending sorted alphabetically. Verified via the accessibility tree rather than screenshots — the Browser pane wasn't compositing frames in this session, a tooling limitation, not an app issue. `npm run lint` and `npm run build` both clean. Test account deleted after. |
| 29 | Bulk-import missing quotations from `_Projects/Quotation 2026` | none (data-only; no code changed) | **verified** | 2026-08-22: requested to import any quotation PDFs from that folder not already tracked. Of 50 quotation numbers found, 46 were already imported in an earlier session. Reused the existing AI PDF-import extraction (`lib/ai/import.ts`) via a throwaway script rather than the browser UI, since importing one-at-a-time through `/projects/import` isn't practical at this volume — grouped files by base quotation code first (stripping revision suffixes), diffed against the live `projects.code` list to find genuinely untracked codes, and only ran AI extraction on those. Added exactly 4 new projects (EVT26QT045–048). **Caught and corrected a real duplication in the same pass**: two other "new" codes (EVT26QT007, EVT26QT033) turned out to be the same quotations as already-tracked BP26QT0012/BP26QT0033 — the filename used a different numbering convention than the number actually printed inside the PDF, which the AI extraction surfaced by reading the document's own stated code. Created, detected, and soft-deleted (`deleted_at`, never hard-deleted per this repo's convention) both duplicates in the same session rather than leaving them. Also fixed a stray `&amp;` HTML entity in one extracted project name (raw text from the source PDF). Workspace ends at 51 active projects (47 + 4, after the 2 duplicates were archived back out). No code or migration changed — this phase is data-only, recorded here for the audit trail. |
| 30 | Show quotation number on the dashboard's Project progress card | `components/dashboard/charts.tsx` (`ProgressChart`) | **verified** | 2026-08-22: requested as 'Dashboard can show "Quotation number" at "Project progress"'. `getDashboard()`'s `progress` list was already the full `Project` type (includes `code`), so this was a display-only change — no data-layer work needed. Prefixed each row's link text with `{code} · ` (muted, matching the `code · client_name` convention already used on the project list and detail pages), and updated the progress bar's `aria-label` the same way so screen reader users get the same information, not just sighted ones. Renders nothing extra when a project has no code (import-created and manually-created projects can both go without one). Verified live on a throwaway project seeded with code `EVT26QT099`: the dashboard row read "EVT26QT099 · Cooling Tower Pipe Support Remediation" and the accessible label read "EVT26QT099, Cooling Tower Pipe Support Remediation: 50% complete, 1 of 2 tasks done". `npm run lint` and `npm run build` both clean. Test account deleted after. |
| 31 | Sort by quotation number (code) on Projects and Budgets | `lib/db/projects.ts` (`ProjectSort` type, `listProjects` sort branch), `app/(app)/projects/page.tsx` (new "Sort by" select in the existing `FilterBar`), `app/(app)/budgets/page.tsx` (new sortable "Quotation #" column) | **verified** | 2026-08-22: requested as "possible to 'Sort' by Quotation number (code) on every tab" — scoped down to the two tabs that actually have a project list to sort (Projects, Budgets) after confirming with the user; Dashboard's progress list is a glanceable top-N widget, Tasks aren't grouped by project code, and Files lists documents rather than projects, so a code sort doesn't apply there. Projects had no sort control at all before this (only status/priority filters) — added "Sort by" as a third `FilterBar` select (Recently updated / Name / Quotation number), URL-driven like the existing filters, so `listProjects` gained a `sort` param feeding a real `.order()` call rather than sorting in memory. Budgets already had sortable columns (phase 28) but didn't display Code as its own column at all — added one, wired into the same click-to-sort/toggle-direction mechanism as the others. Both places push projects with no code to the end regardless of sort direction, same rule as phase 28's `used` column — there's no meaningful rank for "no quotation number". Verified live on a throwaway account seeded with three coded projects (`EVT99QT001/002/003`) plus one uncoded project: Projects page sorted exactly `QT001 → QT002 → QT003 → (no code)` with "Sort by" showing "Quotation number" selected; Budgets page sorted the same set descending as `QT003 → QT002 → QT001 → (no code)`, confirming the no-code row stays last in both directions. `npm run lint` and `npm run build` both clean. Test account deleted after. |

## Real data (2026-08-06)

Imported the actual "Repair Foundation CT Pipe Support Block 1" project
(contract ETOR-2601058, client Gulf JP NS Co., Ltd.) into `pepe's workspace`
(owner `bozosx@gmail.com`) from the standalone HTML tracker at
`Pipe Support fix for Cooling Tower/Project Tracker.html` — the source this
session's phase 21 features were built to cover. Parsed directly out of that
file's embedded JSON (not hand-transcribed, to avoid typos in 22 tasks and a
22×36 QC grid) and inserted in one transaction. Confirmed after: 22 tasks (19
done, 3 blocked — the ones marked failed QC needing rework), one budget line
("Contract Value", ฿575,944 — the source tracked one lump figure, not
itemized lines), 36 QC units (`CT-01`..`CT-36`), 22 QC items, 664 QC results
(644 pass / 20 fail), and `progress_pct` correctly auto-computed at 86% by
the existing trigger. The source's Materials and Status Log were both empty
(nothing to import); the source's per-task `category` field (Structural,
Coating/Paint, etc. — this app has no equivalent column) and its always-zero
est/actual cost fields were folded into each task's `description` rather than
dropped or given a schema change neither the app nor the real data needed.


## Decisions (settled — do not relitigate)

| Decision | Choice | Why |
|---|---|---|
| Auth | ~~Open access~~ → **Supabase Auth, restored** (reversed again 2026-08-05, before deploying) | Real email/password sign-in; RLS in `0006_rls.sql` is the actual gate again, not just documented. `0013_restore_auth.sql` reverses `0012_open_access.sql`. Prompted directly by "deploy it" — open access was fine on localhost but would have made every project, budget, and file public on a real URL. See AGENTS.md's Authentication section. |
| Database | **Dedicated Supabase project, `tracker` schema** (changed 2026-08-05) | Originally a shared project (matching visa-agency's `agency`, bakery-pos's `bakery`). Reversed after the shared project died in DNS and took five sibling apps down with it — this app now owns its project outright, so no other app's outage or config can affect it. Schema name kept for convention, not because it's shared. |
| AI provider | **Anthropic Claude API** (`@anthropic-ai/sdk`) | Model defaults to `claude-opus-5`, overridable with `ANTHROPIC_MODEL`. Cost is controlled with `effort` (`medium` for narrative kinds, `high` for the risk scan) rather than by silently picking a smaller model. |
| Word reports | **Generated from code with `docx`** | Full control, no template file to keep in sync. `Unit Report Template (editable).docx` is a layout reference only. |
| Multi-user | **`workspace_id` + `workspace_members` from day one** | Single user today is one workspace with one member. Adding people stays a data change. |
| Next version | **16.3.0**, not the siblings' 16.2.10 | 16.2.10 carries nine high-severity advisories (SSRF in server actions, cache confusion, unauthenticated server-function disclosure). The conventions that matter — `proxy.ts`, `--webpack` — are identical. |

## Fixed: `created_by`/`owner_id`/`assignee_id`/`actor_id` foreign-key bug (2026-08-05)

**Found by actually using the app**, not by reading code: submitting the real
"New project" form threw `insert or update on table "projects" violates
foreign key constraint "projects_owner_id_fkey"` right on screen. Direct-SQL
verification (used for phases 4–7 earlier the same day) never exercises the
app's own server actions, so it missed this entirely — a reminder that SQL
checks and clicking the real UI catch different bugs.

**Root cause:** `0012_open_access.sql` made `workspaces.created_by` nullable
when it removed authentication, but missed every other column with the same
`references auth.users(id)` foreign key: `projects.owner_id`,
`projects.created_by`, `tasks.created_by`, `tasks.assignee_id`,
`ai_summaries.created_by`, `report_jobs.created_by`, `activity_logs.actor_id`.
The anonymous session's fixed id (`lib/auth.ts`'s `ANONYMOUS.id`) has never
existed in `auth.users`, so every write into one of those columns violated
its foreign key.

**Blast radius:** project creation failed outright (thrown, user-facing).
Task creation had the identical pattern. AI summary caching and the activity
log both *appeared* to work — the AI panel still streamed real answers, the
UI showed no error — because both call sites swallow the insert error rather
than surfacing it, so the failure was invisible: summaries silently never
cached (re-billing the API on every view) and the activity log stayed
permanently empty.

**Fix (anonymous era):** every affected insert wrote `null` for these columns
instead of `user.id` — the columns are already nullable for exactly this
reason. Touched `lib/audit.ts`, `lib/ai/summarize.ts` (+ removed the then-dead
`userId` param from `saveSummary`/`generateSummary` and their call sites),
`app/(app)/projects/actions.ts`, `app/(app)/tasks/actions.ts`,
`app/(app)/budgets/actions.ts`, `app/api/reports/word/route.ts`. Re-verified
live at the time: project creation, task creation, AI summary caching
(generate → cache-hit → invalidate-on-edit), and the activity log all
confirmed working through the actual app, not just SQL.

**Superseded same day, once auth was restored:** all six `null` writes above
were reverted back to `user.id`/`input.userId` — with real `auth.users` rows
now backing every session, the original code was correct all along and the
`userId` params came back too. Re-verified live again: `owner_id`/
`created_by`/`actor_id` on a freshly created project all show the real
signed-in user's UUID, not `null`.

## Blockers

**Resolved 2026-08-05.** The dedicated Supabase project is live, all migrations
are applied, and the schema is exposed. Remaining work is walking the
verification checklist below, phase by phase.

<details>
<summary>History (kept for context)</summary>

**Previously: waiting on a live Supabase project.** The old shared project
(`lfpsnhuarpdlzrsnycmo.supabase.co`) stopped resolving in DNS on 2026-08-04
(confirmed against 8.8.8.8, not a local resolver problem) and the session
pooler rejects the tenant with `tenant/user not found`. It was shared with
five sibling apps (daily-budget-app, project-list-app, visa-flow, visa-agency,
bakery-pos), all of which went down with it.

**Decision made 2026-08-05: this app gets its own dedicated Supabase project**,
not a shared one — see the Database row above. `scripts/apply-migration.mjs`
and `scripts/check-schemas.mjs` no longer hardcode a project ref or read a
password file from a sibling app's directory; both now read a single
`SUPABASE_DB_URL` from `.env.local` (see `.env.local.example`).

**Still needed:** the project itself. Create one at the Supabase dashboard (or
provide a Personal Access Token so it can be created via the Management API),
then fill in `NEXT_PUBLIC_SUPABASE_URL`, `NEXT_PUBLIC_SUPABASE_ANON_KEY`,
`SUPABASE_SERVICE_ROLE_KEY`, and `SUPABASE_DB_URL` in `.env.local`. Once
that's done: `npm run migrate -- --all`, then `npm run check-schemas`.

</details>

### To verify against the live database

1. `npm run migrate -- --all`, then `npm run check-schemas` — confirms the
   `tracker` schema is exposed to PostgREST.
2. Register an account. Confirm the signup trigger (`0007_signup.sql`,
   reactivated by `0013_restore_auth.sql`) created a workspace and the sidebar
   shows it with role Owner. **Verified 2026-08-05** via the Admin API +
   real `/login` form (email confirmation bypassed for testing with
   `email_confirm: true`, since there's no inbox to check here) — the trigger
   correctly provisioned `"<name>'s workspace"` with Owner role.
3. Sign out from the topbar — confirm it lands on `/login` and does not bounce
   back (`AUTH_ROUTES` in `lib/supabase/middleware.ts` exists for exactly
   this). **Verified 2026-08-05.**
4. Delete the membership row by hand and reload — `ensure_workspace()` should
   repair it rather than showing an empty screen. Not yet re-verified since
   auth was restored.
5. Sign in as a second account and confirm RLS refuses it. **Verified
   2026-08-05**: a second account, in its own separate workspace, read back
   `0 projects` for a project that genuinely existed under the first
   account's workspace — real isolation, not just a documented intent. Also
   confirm `SUPABASE_SERVICE_ROLE_KEY` never reaches the browser bundle (it's
   no longer even read by the running app, only by admin scripts) —
   `Get-ChildItem .next\static -Recurse -Include *.js | Select-String "service_role","SUPABASE_SERVICE_ROLE_KEY"`
   must find nothing.
6. Phase 4: create a project, add tasks, mark one done — `progress_pct` must
   move on its own (the trigger computes it; the app never writes it). Add an
   overdue task and confirm health flips to at risk, then off track past the
   target date.
7. Confirm `tracker.project_overview` is declared `security_invoker = true` in
   the migration — without it, the view would run as its owner and bypass RLS,
   handing every workspace's rows to every signed-in user regardless of
   membership. Directly relevant now that multi-user auth is live; step 5's
   isolation test above already covers the practical case.
8. Phase 5: add two budget lines and several expenses, then check the totals by
   hand against the rows. The views aggregate each child table in its own
   subquery before joining — if that were ever flattened into direct joins,
   tasks and expenses would multiply against each other and every sum would be
   inflated. A project with both tasks and expenses is the case that catches it.
9. Record an expense with no budget line and confirm it shows as *Unbudgeted*
   and lands in `spent_unassigned`, not silently in a line.
10. Delete a budget line that has expenses charged to it. The expenses must
    survive as unassigned (FK is `on delete set null`), not disappear.
11. Confirm the expense/budget-line project guard fires: charging an expense to
    a line from another project must be refused by the trigger.
12. Phase 6: upload a file, then fetch its raw storage URL while signed out —
    it must 403. A public bucket here would expose every customer document.
13. Force the metadata insert to fail (temporarily break `recordDocument`) and
    confirm the uploaded object is removed again. An orphaned blob is invisible
    in the UI and can only be found from the Supabase dashboard.
14. Upload the same filename to the same project twice. The second must become
    v2 with `replaces_id` pointing at v1, not restart at v1 — the NULL-safe
    branch in the version lookup is what makes workspace-level files work.
15. Delete a file and confirm both the row and the object are gone.
16. Upload as a viewer: the storage RLS policy must refuse it, not just the UI.
    Also try a file over 20 MB and a disallowed type — the bucket enforces both
    independently of `validateFile()`.
17. Phase 7: check the dashboard totals against the tables by hand. `getDashboard`
    counts open and overdue tasks by summing `project_overview` across live
    projects — that is only right if the view's per-project counts are right,
    so step 8 has to pass first.
18. Put one project over budget and eyeball the meter in both themes. Over
    budget the track changes meaning: it becomes total spend, split into
    planned and overspend. Confirm 130% and 400% look clearly different from
    each other.
19. Give a project a very long name and confirm it truncates rather than
    colliding with the value on the right.
20. Phase 8: set `ANTHROPIC_API_KEY`, open a project, run each summary kind.
    Confirm the "Thinking…" state appears and is replaced by streaming text.
21. Run the same summary twice without changing the project — the second must
    come back instantly marked *Cached*. Then edit one task and re-run: it must
    regenerate, proving the hash tracks the snapshot.
22. Confirm a summary never states a fact absent from the project. The prompt
    forbids it, but this is the failure mode that matters most in a tracker.
23. Re-run the bundle scan after any change to `lib/ai/`:
    `Get-ChildItem .next\static -Recurse -Include *.js | Select-String "ANTHROPIC_API_KEY","sk-ant-"`
    must find nothing.
24. Phase 9: `npm run report:preview` after any change to `lib/reports/word.ts`,
    and open the result. The fixture covers the awkward cases — overdue task,
    over-budget line, unbudgeted spend — and the validator only checks that the
    file is well-formed, not that it *looks* right.
25. Download a report from a real project and confirm the figures match the
    screen. Check a project with no milestones and no budget renders those
    sections as "not recorded" rather than as empty tables.
26. Confirm `?ai=0` produces a report with no executive summary or risks
    section, and that a project downloads successfully when
    `ANTHROPIC_API_KEY` is unset — the AI sections are best-effort and must
    never be the reason a download fails.
27. Phase 10: `/demo` renders the real components from fixture data with no
    database, so the dashboard, project detail, budget table and kanban can all
    be reviewed at 375 / 768 / 1280 without signing in. **This already caught a
    real bug** — `Card` had no `min-w-0`, so as a grid child it refused to
    shrink and the dashboard scrolled sideways at 375px. Re-check `/demo` after
    any layout change. The remaining gap is the signed-in shell (sidebar,
    drawer, topbar), which the demo does not exercise. Re-run it on the
    dashboard, project detail, the kanban board and the budget tables at
    375 / 768 / 1280, in both themes. The tables are the likely offenders:
    each sits in an `overflow-x-auto` wrapper, so the table should scroll on
    its own without the page body scrolling sideways.
28. Keyboard pass: tab from the top of an `(app)` page. "Skip to content"
    should appear first and jump past the sidebar. On a narrow viewport, open
    the drawer and confirm Escape closes it.
29. Confirm the loading skeletons actually appear. They only show while a route
    segment streams, so a fast local database may skip them entirely — throttle
    the network to see them.

## Deployed (2026-08-05)

Live at **https://ai-project-tracker-zeta.vercel.app** (Vercel project
`bozosx-debugs-projects/ai-project-tracker`). `NEXT_PUBLIC_SUPABASE_URL`,
`NEXT_PUBLIC_SUPABASE_ANON_KEY`, and `ANTHROPIC_API_KEY` are set as Production
env vars in Vercel — `SUPABASE_SERVICE_ROLE_KEY`/`SUPABASE_DB_URL` deliberately
are not, since the deployed app never reads them. Verified live:
unauthenticated `/dashboard` redirects to `/login`; `/demo` stays reachable
without signing in.

**Fixed 2026-08-05: email confirmation links were completely non-functional,
not just pointed at the wrong domain.** The owner reported clicking a
confirmation link and landing on `localhost:3000/?code=...`. Two separate
bugs, both now fixed/flagged:

1. **Missing code exchange (real bug, now fixed).** Nothing in the app ever
   turned that `?code=` into a session — there was no callback route at all.
   `app/page.tsx` just did `redirect("/dashboard")`, silently dropping the
   code, and `proxy.ts` would have bounced the (still-signed-out) request to
   `/login` before the page even ran. Fixed in `lib/supabase/middleware.ts`:
   `updateSession()` now checks for a `code` query param on every request and
   calls `exchangeCodeForSession()` before any redirect logic runs — a Server
   Component can't set cookies itself, so the middleware is the only place
   that can turn the code into a signed-in session cookie. Verified: an
   invalid/expired code cleanly redirects to `/login` rather than crashing;
   the success path follows Supabase's own documented pattern for this exact
   scenario. Full round-trip with a real code couldn't be tested here — it
   requires the *same browser* that submitted the sign-up form (the PKCE
   `code_verifier` lives in a cookie only that browser has), and there's no
   real inbox to click a link from in this environment.
   **Known limitation worth being aware of:** if a user signs up on one
   device/browser and opens the confirmation email on another, this exchange
   will fail (`code_verifier` won't match) — that's inherent to PKCE + email,
   not something this fix can close. Supabase's recommended fix is switching
   the email template to a `token_hash`-based `/auth/confirm` route instead
   of the default `?code=` link, which needs a dashboard template edit (see
   below) — not done, since it's a bigger change than "fix the redirect."
2. **Site URL still needs manual action on the Supabase dashboard** —
   Authentication → URL Configuration → set Site URL (and add a Redirect URL)
   to `https://ai-project-tracker-zeta.vercel.app`. Not doable from here.

## Open items

- Import "won" projects from `project-list-app` into the tracker — revisit after
  phase 4 proves the data model. Not in scope now.
- GitHub remote not created. Repo is local-only (`git init` in phase 1). Vercel
  deploys were pushed straight from the local directory via `vercel --prod`,
  which works but means there's no CI and no PR-preview workflow.
- **React hydration error `#418` on every page of the production deployment
  specifically** (see phase 22). Reproduces on unrelated pages, not in dev,
  not in an identical local production build — so it's something about the
  Vercel deployment itself rather than app code, unconfirmed which. Doesn't
  appear to block anything observable (React recovers by re-rendering
  client-side), but is a real console error on every page load and worth
  a proper look with source maps / Vercel's own function logs rather than
  guessing further.
